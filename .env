# Server workspace path — edit this before running setup.sh
# Format: /root/<your-username>/workspace  (or any absolute path on the GPU server)
# Used by: compose.yml, all scripts in scripts/, setup.sh
SERVER_WORKSPACE=/root/skurchev/workspace
